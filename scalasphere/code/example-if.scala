40 columns                              |
val newColumn = if(split == Newline) newIndent
                else column + split.length
>>>
val newColumn =
  if (split == Newline) newIndent
  else column + split.length
